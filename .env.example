# .env.example
# -----------------------------------------------------------------------------
# Portfolio App - Environment Variable Contract
#
# IMPORTANT:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# - Do NOT place secrets, tokens, private endpoints, or sensitive data here.
# - Copy this file to .env.local for local development (do not commit .env.local).
# -----------------------------------------------------------------------------

# Canonical site URL (optional but recommended for metadata/canonical links).
# Use your production domain in Vercel Production.
# Example: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=

# Documentation App base URL (recommended).
# Set to either:
# - Path strategy: https://yourdomain.com/docs
# - Subdomain strategy: https://docs.yourdomain.com
#
# If not set, the app will default to "/docs" (works only if docs truly live there).
NEXT_PUBLIC_DOCS_BASE_URL=

# Public profile links (optional but recommended)
# Example: https://github.com/your-handle
NEXT_PUBLIC_GITHUB_URL=
# Example: https://www.linkedin.com/in/your-handle/
NEXT_PUBLIC_LINKEDIN_URL=

# Optional public contact email (mail link only; no form).
# If omitted, prefer GitHub/LinkedIn as the contact surface.
NEXT_PUBLIC_CONTACT_EMAIL=

# -----------------------------------------------------------------------------
# Analytics (Vercel Web Analytics - Stage 3.6)
# -----------------------------------------------------------------------------
# Vercel Web Analytics is automatically enabled when deployed to Vercel.
# No configuration required. Analytics are privacy-safe:
# - No cookies
# - No personal identifiable information (PII) collected
# - Aggregate page view data only
# - GDPR compliant
#
# View analytics: https://vercel.com/[your-team]/[project]/analytics
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Vercel note:
# Vercel sets VERCEL_ENV automatically (production | preview | development).
# Do not set VERCEL_ENV manually.
# -----------------------------------------------------------------------------
